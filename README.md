# 🌐 陳竑齊（Hung‑Chi, Justin）個人網站

使用 **Next.js 14 App Router + TypeScript + React + Tailwind CSS** 打造的個人網站，主要用來介紹自己的生活、專長興趣，整理旅行紀錄，以及講講我曾經做過的事。

---

## 🛠 技術棧

- **框架**：Next.js 14+（App Router）
- **語言**：TypeScript
- **前端**：React 18
- **樣式**：Tailwind CSS + 自訂 CSS（CSS 變數、動畫）
- **圖片優化**：`next/image`
- **表單處理**：Next.js Route Handler（`app/api/contact/route.ts`）
- **字體**：Inter（Google Fonts）
- **圖標**：Font Awesome 6.0

---

## 📄 頁面與功能

- **首頁 Home (`/home`)**

  - 個人頭像與名字（陳竑齊）
  - 標題輪播動畫（例如：臺灣大學資訊管理學系、SITCON2025 行銷組、努力賺錢中）
  - 個人標籤：投資小白、網頁開發、資料分析、排球、攝影、桌遊
  - 常用社群連結：Email、GitHub、LinkedIn、Instagram
- **關於我 About (`/about`)**

  - 聯絡資訊：Email、電話、GitHub、LinkedIn（一鍵複製 Email / 電話）
  - Hard Skills / Soft Skills 標籤
  - 學歷時間軸（成功高中 → 臺灣大學資訊管理學系）
  - 語言能力進度條動畫（中文 / English）
  - 活動與工作經驗時間軸（營隊、社群活動、家教、RA 等）
  - 興趣卡片：攝影、桌遊、排球（含圖片與角落小標）
- **旅行紀錄 Travel (`/travel`)**

  - 旅行卡片：Japan、Hong Kong & Macau、Australia (Coming Soon)
  - 點擊卡片開啟 Modal：顯示日期、行程簡介、Highlights、照片牆
  - 使用 `IntersectionObserver` 建立滾動進場動畫
  - Back to Top 浮動按鈕
- **聯絡我 Contact (`/contact`)**

  - 卡片式聯絡方式：Email、Phone、GitHub、LinkedIn
  - 含驗證的聯絡表單（姓名 / Email / 主旨 / 訊息）
  - 前端即時欄位驗證與錯誤提示
  - 表單送出時呼叫 `/api/contact`，顯示成功 / 失敗訊息
- **共通體驗**

  - 深色 / 淺色主題切換，使用 `localStorage` 記住偏好並支援系統偏好（`prefers-color-scheme`）
  - 多處滾動進場動畫與 hover 動畫（卡片浮起、圖片放大等）
  - RWD 設計：桌機、平板、手機均可舒適瀏覽

---

## 📁 專案結構

```text
personal-website/
├── app/
│   ├── layout.tsx              # 全域 Layout（Navbar + Footer）
│   ├── page.tsx                # 根路由：redirect 到 /home
│   ├── globals.css             # 全域樣式與 Tailwind 載入
│   ├── home/
│   │   ├── page.tsx            # 首頁
│   │   └── home.css            # 首頁樣式
│   ├── about/
│   │   ├── page.tsx            # 關於我
│   │   └── about.css
│   ├── travel/
│   │   ├── page.tsx            # 旅行紀錄
│   │   └── travel.css
│   ├── contact/
│   │   ├── page.tsx            # 聯絡我
│   │   └── contact.css
│   └── api/
│       └── contact/
│           └── route.ts        # 聯絡表單 API
├── components/
│   ├── Navbar.tsx              # 導覽列（含手機版漢堡選單）
│   ├── Footer.tsx              # 頁尾
│   ├── ThemeToggle.tsx         # 主題切換按鈕
│   └── TitleAnimation.tsx      # 首頁標題輪播 + 點點指示
├── hooks/
│   ├── useTheme.ts             # 深淺色主題管理
│   └── useScrollAnimation.ts   # 滾動動畫 Hook（目前部分頁面使用原生 Effect）
├── public/                     # 靜態圖片資源（頭像、旅行照片等）
├── styles/
│   └── globals.css             # Tailwind 指令與 CSS 變數
├── tailwind.config.ts
├── tsconfig.json
├── next.config.js
└── package.json
```

---

## 🚀 開發與執行方式

### 1. 安裝依賴

```bash
npm install
```

### 2. 啟動開發伺服器

```bash
npm run dev
```

開啟瀏覽器造訪 `http://localhost:3000`，首頁會自動導向 `/home`。

### 3. 建置生產版本

```bash
npm run build
npm start
```

---

## ☁️ 部署與上線（Vercel）

本專案已部署在 Vercel，實際線上版本可由此查看：

- **Production URL**：[`chc-website-delta.vercel.app`](https://chc-website-delta.vercel.app)

此專案已按照 Next.js 規範配置，可直接部署到 [Vercel](https://vercel.com)：

1. 將 Repo 推上 GitHub / GitLab / Bitbucket。
2. 在 Vercel 中「Import Project」，選擇此 Repo。
3. 保持預設的 Next.js 設定，Vercel 會自動建置與部署。
4. 之後每次 push 都會自動觸發重新部署。

---

## 👤 作者

- **Name**：陳竑齊（Hung‑Chi, Justin）
- **Email**：`chenjustin824@ntu.im`
- **GitHub**：`chennjustinn`
- **LinkedIn**：Hung‑Chi Chen

---

© 2025.09 陳竑齊. 保留所有權利。
